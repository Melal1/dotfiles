# import some var 

source ~/.config/ZSH/core/variables

# imortwallpapers

# Hyprwall=/home/melal/.config/hypr/Src/Wallpapers

# wallpaper

swww init &
sleep 3 &
swww img -o HDMI-A-1 --transition-type center --transition-fps 75 --transition-duration 2 $Wall1 &
swww img -o DP-1 --transition-type center --transition-fps 60 --transition-duration 2 $Wall2 &


# clipboard manager

wl-paste --watch cliphist store &
# waybar 
waybar &

# other
#


dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP &
